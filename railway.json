{
  "$schema": "https://railway.com/railway.schema.json",
  "deploy": {
    "preDeployCommand": "python manage.py migrate --noinput",
    "startCommand": "gunicorn mosaicopixel.wsgi:application --bind 0.0.0.0:$PORT"
  }
}
